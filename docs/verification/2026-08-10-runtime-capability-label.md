# 运行能力状态标签验收（2026-08-10）

首页运行状态中的 capability 布尔值表示“当前能否使用”，不是实现进度。例如 TMDB
未配置凭据，或旧下载器迁移门禁阻止统一导入/qB 时，对应值会是 `false`，但模块本身
已经实现。

WebUI 现将 `true` 显示为“已启用”，将 `false` 显示为“当前不可用”，不再错误显示为
“待实现”。静态资源测试同时断言新标签存在、旧条件标签不存在。
