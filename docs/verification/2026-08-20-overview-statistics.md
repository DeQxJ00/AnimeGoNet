# 总览关键统计与跳转验收（2026-08-20）

## 实现口径

- 总览按“下载与整理”“匹配与人工处理”“资产与运行规模”展示 14 个直观统计数字。
- 数字复用下载任务、匹配任务、作品库、待补全 TMDB、输入源五个现有权威 API，不建立新的前端业务计数。
- 五类请求并行执行且允许部分成功；单个接口失败只把对应统计标记为读取失败。
- 下载统计按钮清除冲突条件并应用与下载列表一致的 `summary_bucket`；Other、匹配错误、等待人工审核复用匹配任务的精确筛选；其余按钮直接进入对应管理页面。
- 总览可见时每 10 秒更新一次，离开页面后不执行该轮询。

## 验证

- `npm run web:check`：通过。
- `npm run web:test`：36/36 通过，覆盖 14 个原生按钮、跳转目标、并行部分失败处理及响应式布局契约。
- `dotnet build AnimeGoNet.slnx -c Release --no-restore`：通过，0 警告、0 错误。
- `StaticWebUiTests` 资产版本定向测试：2/2 通过。
- 本机真实 WebUI `http://127.0.0.1:6180/#/overview/status`：成功显示活动 39、暂停 2、失败 1、等待整理 21、已完成 58、快照过期 4、动画季度 27、匹配任务 80、已启用来源 1、离线下载器 0 等当前运行数据。
- 浏览器点击“下载/整理失败”后进入 `#/tasks/downloads` 且列表显示“快捷筛选：失败”；点击“待补全 TMDB”进入 `#/library/pending`；点击“已启用来源”进入 `#/sources/manage`。验收只读，未修改任务、下载器或媒体文件。
