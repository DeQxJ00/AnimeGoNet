# 忽略 Other 待处理

- 已整理且没有正在执行重新适配的任务可以将当前全部 Other 人工标记为 `ignored`。
- 操作不会移动或删除已经位于 `Extras` 的文件，原 `other_reason` 保留用于审计。
- 标记后不再计入 Other 待处理、Other 筛选或重新适配候选。
- WebUI 在“匹配与整理”任务卡的 Other 操作区提供“忽略处理”，提交前明确确认。

验收：API 覆盖成功、状态持久化、原因保留；WebUI 静态契约覆盖按钮与接口。
